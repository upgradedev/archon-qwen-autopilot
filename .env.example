# ── Qwen / Alibaba Cloud Model Studio (DashScope) ────────────────────────────
# Create a key in the Model Studio console. When absent, the app auto-falls back
# to the deterministic offline Fake chat client + Fake embedder, so the WHOLE
# workflow (intake → decide → approve → execute) runs in dev + CI with NO key.
DASHSCOPE_API_KEY=

# OpenAI-compatible base URL. Default is the hackathon international endpoint.
# China (Beijing) alternative: https://dashscope.aliyuncs.com/compatible-mode/v1
# Workspace-scoped form also works: https://<WorkspaceId>.ap-southeast-1.maas.aliyuncs.com/compatible-mode/v1
DASHSCOPE_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# Qwen chat model that drives the multi-step ReAct decision loop.
QWEN_MODEL=qwen-plus
# Versioned high-upside candidate (promote only after frozen 3-run decision + vision A/B):
# QWEN_MODEL=qwen3.7-plus-2026-05-26
# The code sets enable_thinking=false explicitly on this model's tool calls.
# Rollback is explicit: restore qwen-plus. There is no silent runtime model substitution.

# Qwen VISION model for real document uploads (POST /intake/document): a PDF/PNG/JPG
# invoice is read into structured fields by this model. Absent a DASHSCOPE key the
# app uses the deterministic FakeExtractionClient, so the upload path runs with no
# key in dev + CI. Default is the flagship DashScope vision model.
VISION_MODEL=qwen-vl-max
# Candidate multimodal/JSON path after the same evidence gate:
# VISION_MODEL=qwen3.7-plus-2026-05-26
# Candidate JSON mode sets enable_thinking=false and omits max_tokens, matching
# Alibaba's structured-output contract. The rollback vision model keeps 2048.
# Rollback is explicit: restore qwen-vl-max.

# Upload guardrails for /intake/document (optional).
# MAX_DOCUMENT_BYTES caps the upload size. PDFs above MAX_PDF_PAGES are rejected
# as a whole; the app never presents a silently truncated extraction as complete.
MAX_DOCUMENT_BYTES=10485760
MAX_PDF_PAGES=3
MAX_PDF_RENDER_DIMENSION=2200
MAX_PDF_RENDERED_BYTES=50331648
# PNG/JPEG header preflight rejects compressed pixel bombs before quota/Qwen.
MAX_IMAGE_DIMENSION=8192
MAX_IMAGE_PIXELS=32000000
# poppler binary used to rasterize PDFs (installed in the Docker image).
POPPLER_PDFTOPPM=pdftoppm
VISION_TIMEOUT_MS=45000
POPPLER_TIMEOUT_MS=20000

# Forwarded client IPs are ignored by default. Enable exactly ONE bounded trust
# mode only when the app is unreachable except through that proxy boundary.
# Prefer exact proxy IP/CIDR values; hop mode is capped at 1–3.
TRUST_PROXY_ADDRESSES=
# TRUST_PROXY_HOPS=1

# Zero-wait provider workflow pools are isolated by caller tier, while document
# extraction/rendering has one aggregate process-wide memory cap across both.
# This ECS deployment runs one 512 MiB app container. For a Function Compute
# topology, also cap instance count so fleet-wide workflow admissions stay bounded.
PUBLIC_PROVIDER_RUN_CONCURRENCY=2
REVIEWER_PROVIDER_RUN_CONCURRENCY=2
DOCUMENT_RENDER_CONCURRENCY=2

# Coarse process-local all-route flood guard (per minute). Durable Postgres daily
# quotas remain the provider-workflow admission authority; this also protects reads/docs/ready.
HTTP_REQUESTS_PER_MINUTE=600
REVIEWER_HTTP_REQUESTS_PER_MINUTE=1200
HTTP_GLOBAL_REQUESTS_PER_MINUTE=5000

# Max steps in the bounded ReAct loop before the safe flag_for_review fallback.
# Optional — defaults to 8 (leaves headroom past the 5 autonomous read/analyze tools).
AUTOPILOT_MAX_STEPS=8

# Qwen text-embedding model + dimension. text-embedding-v4 defaults to 1024,
# which matches the vector(1024) memory column. Keep the two in lockstep.
QWEN_EMBED_MODEL=text-embedding-v4
EMBED_DIM=1024

# ── Memory store: pgvector-compatible PostgreSQL ─────────────────────────────
# Local dev / CI: a stock pgvector/pgvector docker image.
# Production: Alibaba Cloud AnalyticDB for PostgreSQL or ApsaraDB RDS for
# PostgreSQL (pgvector extension) — same pg-wire protocol, same SQL.
# When unset, the server can still run with in-memory stores (offline demo/tests).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
PGPOOL_MAX=5

# Production authenticity is fail-closed: NODE_ENV=production requires both
# real Qwen credentials and DATABASE_URL. These escape hatches are for an
# explicitly ephemeral/offline demonstration only; leave false in judging/prod.
ALLOW_FAKE_QWEN=false
ALLOW_IN_MEMORY_STORE=false

# Daily upload caps for the open demo (POST /intake · /intake/stream ·
# /intake/document · /extract/document share one budget, per UTC day). Protects the
# number of workflows that may reach Qwen on an unauthenticated endpoint. Two tiers,
# both optional:
#   UPLOAD_DAILY_LIMIT        — per-client (per-IP) cap.       Code default: 100.
#   UPLOAD_GLOBAL_DAILY_LIMIT — hard global backstop across clients. Code default: 2000.
# It is independent and may be lower than the per-client cap; the app never widens it.
# The example values below are a small, safe demo setting; raise them for a judging
# window. The global backstop is a hard public workflow-entitlement bound (the
# per-client key is best-effort behind a proxy — see src/ap/rate-limit.ts). It is not
# a token/billing bound: steps, pages and deadlines bound each admitted workflow,
# while transparent provider retries are not counted as additional quota units.
UPLOAD_DAILY_LIMIT=100
UPLOAD_GLOBAL_DAILY_LIMIT=2000
# A valid REVIEWER_TOKEN uses this independent, still-bounded reserve. Public
# traffic cannot consume it. Maximum accepted public + reviewer workflows/day is
# capped by the sum of both globals; actual provider calls/tokens are bounded separately.
REVIEWER_UPLOAD_DAILY_LIMIT=250
REVIEWER_UPLOAD_GLOBAL_DAILY_LIMIT=500

# ── Vendor-reply email sink (real SMTP) ──────────────────────────────────────
# One of the two configurable real terminal-action sinks: when a human approves a
# draft_vendor_reply, SmtpEmailSink (src/ap/smtp-sink.ts) submits the approved message
# to the configured SMTP transport and awaits transport acceptance; recipient delivery
# is not claimed. All settings are OPTIONAL — when SMTP_HOST is unset the app uses the
# in-memory Fake email sink and submits nothing, so dev + CI stay provider-offline.
#   SMTP_HOST   — SMTP server hostname. UNSET → simulate (send nothing).
#   SMTP_PORT   — default 587 (STARTTLS). Use 465 with SMTP_SECURE=true for implicit TLS.
#   SMTP_SECURE — true → implicit TLS (port 465); default false → STARTTLS.
#   SMTP_USER / SMTP_PASS — optional auth credentials; supply BOTH or startup fails.
#   SMTP_FROM   — required unless non-empty SMTP_USER provides the fallback.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# Strict SMTP I/O deadlines. Their maximum combined window (120s) remains below
# the minimum 5-minute recovery lease, preventing lease takeover during SMTP I/O.
SMTP_CONNECTION_TIMEOUT_MS=10000
SMTP_GREETING_TIMEOUT_MS=10000
SMTP_SOCKET_TIMEOUT_MS=30000
EXECUTION_RECOVERY_LEASE_MS=600000

# ── Journal-entry ledger sink (real durable JSONL) ───────────────────────────
# The SECOND real terminal-action sink: when a human approves a draft_journal_entry,
# JsonlLedgerSink (src/ap/ledger-sink.ts) appends the approved double-entry accrual —
# one JSON object per line — to a real, durable, append-only JSONL ledger file. OPTIONAL:
# when LEDGER_JSONL_PATH is unset the app uses the in-memory Fake ledger sink and NOTHING
# is written, so dev + CI stay offline. Setting it switches the journal-entry action to a
# real durable append, still strictly behind the human approval gate. A Postgres-backed
# ledger would drop in behind the same LedgerTransport seam.
#   LEDGER_JSONL_PATH — path to the append-only JSONL ledger. UNSET → simulate (write nothing).
#   LEDGER_DEDUPE_SCAN_BYTES — bounded legacy-tail scan before sidecar markers (default 8 MiB).
# The real transport fsyncs both the JSONL row and a per-work-item sidecar marker;
# completed refs deduplicate after restart, while marker-without-row requires
# explicit reconciliation rather than risking a second posting.
LEDGER_JSONL_PATH=
LEDGER_DEDUPE_SCAN_BYTES=8388608

# ── Optional MCP stdio proposal/read surface ────────────────────────────────
# Disabled by default in production. MCP never exposes approve/amend/reject.
ENABLE_MCP_STDIO=false
MCP_TENANT_ID=
MCP_FULL_REVIEWER_EVIDENCE=false
MCP_DAILY_LIMIT=200
MCP_GLOBAL_DAILY_LIMIT=1000
MCP_MAX_RESPONSE_BYTES=524288

# ── HTTP server ──────────────────────────────────────────────────────────────
# Function Compute custom-container default CAPort is 9000.
PORT=9000

# Reviewer boundary. Generate at least 32 random bytes, share it privately with
# judges, and enter it in the UI's "Judge reviewer token" field. /pending,
# /decided, /approve, /amend, /reject, and /recover fail closed without it. When
# the UI sends the same valid token on intake/upload it uses the bounded reviewer
# reserve above; invalid tokens remain on public quota.
REVIEWER_TOKEN=
REVIEWER_NAME=hackathon-judge

# A claim without a recorded sink failure must age this long before manual
# crash-recovery is permitted. Explicit reconciliation is still required.
EXECUTION_RECOVERY_AFTER_MS=300000

# Qwen-VL reads below this confidence are deterministically routed to review.
# This is distinct from the decision model's proposed-action confidence.
EXTRACTION_REVIEW_THRESHOLD=0.60

# Exact cross-origin allowlist (comma-separated). Empty = same-origin only.
# Wildcard '*' is rejected. The production same-origin UI needs no CORS entry.
CORS_ORIGINS=

# JSON intake bound; multipart document uploads have MAX_DOCUMENT_BYTES above.
MAX_JSON_BODY_BYTES=262144

# Public /ready checks reviewer auth + DB + provider configuration without a
# provider call. Authenticated /ready/deep is additionally protected by reviewer
# quota + provider admission and performs one live embedding probe.
READY_REQUIRE_QWEN=true
READY_REQUIRE_DATABASE=true
