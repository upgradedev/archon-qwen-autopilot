# dependencies
node_modules/

# build output
dist/

# code-coverage output (c8 / nyc)
coverage/
.nyc_output/

# Playwright e2e artifacts
test-results/
playwright-report/
blob-report/
.playwright/

# k6 load-test summary artifact (regenerated by `npm run load` / the load-test workflow)
load-summary.json

# env / secrets — never commit
.env
.env.*
!.env.example
!.env.migration.example

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Serverless Devs local state
.s/

# Claude Code local worktrees/settings (never commit)
.claude/

# readiness gate output (regenerated by scripts/readiness.ts; CI uploads it as an artifact)
/readiness.json

# demo-video build artifacts (regenerated by scripts/build_video.py / CI)
.buildwork/
.artifacts/
demo/.private-captures/
demo/private-originals/
/scenes.mp4
/voiceover.mp3
demo/video/voiceover.mp3
__pycache__/
*.pyc
